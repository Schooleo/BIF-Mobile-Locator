<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BIFLocator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Background -->
        <item name="colorScreenBackground">@color/white</item>

        <!-- Main brand color -->
        <item name="colorPrimary">@color/primary_green</item>

        <!-- Text on background -->
        <item name="colorOnBackground">@color/black</item>

        <!-- Surface (card / box UI) -->
        <item name="colorSurface">@color/light_mode_box</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorSurfaceBox">@color/light_mode_box</item>

        <!-- Text on surface -->
        <item name="colorInputText">@color/black</item>

        <!-- Semantic color aliases -->
        <item name="colorHintText">@color/primary_grey</item>
        <item name="colorAuthLink">@color/auth_link_color</item>
        
        <!-- Social screen colors -->
        <item name="colorListItemBackground">@color/item_background</item>
        <item name="colorTextPrimary">@color/black</item>
        <item name="colorTextSecondary">@color/text_secondary</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.BIFLocator.MaterialAlertDialog</item>
        <item name="popupMenuStyle">@style/Widget.BIFLocator.PopupMenu</item>

    </style>

    <!-- Tab Text Style -->
    <style name="TabTextStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">normal</item>
    </style>

</resources>