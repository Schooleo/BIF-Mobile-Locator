type Query {
    getAllProducts: [Product]
    getProduct(id: ID!): Product
}

type Mutation {
    createProduct(input: ProductInput!): Product
}

type Product {
    id: ID!
    name: String!
    price: Float!
}

input ProductInput {
    name: String!
    price: Float!
}
