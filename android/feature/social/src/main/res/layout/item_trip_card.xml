<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/social_trip_card_margin_horizontal"
    android:layout_marginVertical="@dimen/social_trip_card_margin_vertical"
    app:cardBackgroundColor="?attr/colorListItemBackground"
    app:cardCornerRadius="@dimen/social_trip_card_corner_radius"
    app:cardElevation="@dimen/social_trip_card_elevation">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/social_trip_card_content_padding">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_trip_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="?attr/colorTextPrimary"
                android:textSize="18sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/btn_more"
                android:layout_width="@dimen/social_icon_button_size"
                android:layout_height="@dimen/social_icon_button_size"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/more_options"
                android:src="@drawable/ic_more"
                app:tint="?attr/colorTextSecondary" />
        </LinearLayout>

        <TextView
            android:id="@+id/tv_date_range"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/social_spacing_xs"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="?attr/colorTextSecondary"
            android:textSize="14sp" />

        <TextView
            android:id="@+id/tv_members_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/social_spacing_xs"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="?attr/colorTextPrimary"
            android:textSize="14sp" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
